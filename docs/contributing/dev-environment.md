# Development environment

The supported local workflow uses:

- Python 3.11
- `uv` for environment and package management
- Node 22 for repo tooling
- Trunk for repo-wide lint and format checks
- Docker for support services and container builds

Run `./scripts/bootstrap-dev.sh` after cloning the repository.

